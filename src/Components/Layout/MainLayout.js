import React from 'react';
import { Outlet } from 'react-router';

const MainLayout = () => {
  /* Router */
  /* State */
  /* Functions */
  /* Hooks */
  /* Render */
  return <Outlet />;
};

export default MainLayout;
