import IndexRouter from 'Pages';
import React from 'react';

const AppPresenter = () => {
  /* Router */
  /* State */
  /* Functions */
  /* Hooks */
  /* Render */
  return <IndexRouter />;
};

export default AppPresenter;
